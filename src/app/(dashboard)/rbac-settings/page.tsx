import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="RBAC Settings"
      description="Roles, permissions, and access policies for staff, managers, and admins."
    />
  );
}
